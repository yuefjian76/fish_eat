/**
 * World configuration for infinite map system
 */
export const WORLD_CONFIG = {
    WIDTH: 20000,
    HEIGHT: 20000,
    SPAWN_X: 10000,     // 出生点 X（世界中心）
    SPAWN_Y: 14000,     // 出生点 Y（深海区域，向上可探索至浅海）
    SPAWN_MARGIN: 500,
    DESPAWN_MARGIN: 2000,
    VIEWPORT_W: 1024,
    VIEWPORT_H: 768,
};